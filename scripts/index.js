//  This is the JS-file for ~weblog.html~  //
console.log('hello world');

let name = 'jakob wilmer';
console.log(name);
// ~name~ (after "let") cannot be a set keyword, such as "let" and "If".
// it should be meaningful and effective
// cannot start with number
// cannot include multiple names or -
